# Blockchain project

## 📚 Описание проекта
Это учебный проект для создания **распределённой блокчейн-системы** с собственной криптовалютой и транзакциями.  
Мы реализуем свой блокчейн на Java с использованием Spring, генерируем кошельки, подписываем транзакции и будем запускать несколько узлов (нод), которые синхронизируют цепочку блоков между собой.

---

## 🔧 Технологический стек
- ☕ **Java 21**
- 🌱 **Spring Boot** (для REST API и DI)
- 📦 **Gradle** (для сборки проекта)
- 🔒 **BouncyCastle** (для генерации ключей и цифровых подписей)
- 🌐 **Docker + Docker Compose** (для поднятия нескольких нод)
- 🐘 (по мере необходимости) **PostgreSQL / MongoDB** для хранения блоков
- 📈 (в будущем) **Prometheus + Grafana** для мониторинга

## 🚀 Как запустить
1. Склонировать репозиторий:
    ```bash
    git clone https://gitlab.com/katacademy-group/blockchain.git
    cd blockchain-project
    ```

2. Собрать проект:
    ```bash
    ./gradlew clean build
    ```

3. Запустить локально:
    ```bash
    ./gradlew bootRun
    ```

4. Поднять несколько нод через Docker Compose (будет позже).

---

## 📝 Дорожная карта
- ✅ Генерация genesis блока
- ✅ Создание и проверка цепочки блоков
- ✅ Подписание транзакций приватным ключом и их валидация
- 🔄 Реализация longest chain и простой консенсус
- 🚀 REST API для работы с блокчейном
- 🕸 Сеть из нескольких нод и их синхронизация
- 🐳 Docker Compose для быстрого развёртывания
- 📈 Мониторинг и графики в Grafana
---
